---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { siteConfig } from '@/config/site';
const base = import.meta.env.BASE_URL;
---

<BaseLayout
  title="Services"
  description="Custom website builds, local SEO, Google Business Profile management, and content strategy designed specifically for pelvic floor physical therapy practices."
  breadcrumbs={[{ name: 'Services', url: '/services/' }]}
>
  <!-- Hero -->
  <section class="relative pt-28 pb-16 overflow-hidden">
    <div class="absolute inset-0">
      <img src={`${base}images/hero-lighthouse.jpg`} alt="" class="w-full h-full object-cover" />
      <div class="absolute inset-0 bg-brand-dark/70"></div>
    </div>
    <div class="container-max relative z-10">
      <div class="max-w-3xl mx-auto text-center">
        <span class="section-label text-brand-accent">Services</span>
        <h1 class="mb-4 text-white">How Beacon Local Helps Your Practice Grow</h1>
        <p class="section-description text-white/80">
          A custom website and ongoing SEO — everything your pelvic floor PT practice needs to get found by more patients online.
        </p>
      </div>
    </div>
  </section>

  <!-- Services Detail -->
  <section class="section-padding">
    <div class="container-max">
      <div class="max-w-4xl mx-auto space-y-12">
        {siteConfig.services.map((service, index) => (
          <div class={`flex flex-col md:flex-row gap-8 items-start ${index % 2 !== 0 ? 'md:flex-row-reverse' : ''}`}>
            <div class="flex-shrink-0 w-16 h-16 bg-brand-primary/10 rounded-2xl flex items-center justify-center">
              <div class="w-8 h-8 text-brand-primary">
                {service.icon === 'search' && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                  </svg>
                )}
                {service.icon === 'code' && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                  </svg>
                )}
                {service.icon === 'pencil' && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                  </svg>
                )}
                {service.icon === 'map' && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                  </svg>
                )}
                {service.icon === 'chart' && (
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                  </svg>
                )}
              </div>
            </div>
            <div class="flex-1">
              <h2 class="text-2xl mb-3">{service.name}</h2>
              <p class="text-brand-dark-light text-lg leading-relaxed">{service.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- What a month looks like -->
  <section class="section-padding bg-surface-alt">
    <div class="container-max">
      <div class="text-center mb-12 max-w-2xl mx-auto">
        <span class="section-label">In Practice</span>
        <h2 class="section-title">What a Typical Month Looks Like</h2>
        <p class="section-description">
          Every month includes a website update, active GBP management, and a recommendations report written specifically for your practice.
        </p>
      </div>
      <div class="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
        <div class="bg-white rounded-2xl overflow-hidden shadow-sm">
          <button class="lightbox-trigger block w-full text-left" aria-label="View website update example full size">
            <img
              src={`${base}images/portfolio-agpt.jpg`}
              alt="Example website content update for a pelvic floor PT practice"
              class="w-full h-48 object-cover object-top cursor-zoom-in"
              loading="lazy"
            />
          </button>
          <div class="p-6">
            <h3 class="text-lg mb-2">Website Update</h3>
            <p class="text-brand-dark-light text-sm leading-relaxed">A new service page section, updated condition list, or refreshed copy — keeping your site current and giving Google fresh content to index.</p>
          </div>
        </div>
        <div class="bg-white rounded-2xl overflow-hidden shadow-sm">
          <button class="lightbox-trigger block w-full text-left" aria-label="View GBP post example full size">
            <img
              src={`${base}images/gbp-post.png`}
              alt="Example Google Business Profile post for a pelvic floor PT practice"
              class="w-full h-48 object-cover object-top cursor-zoom-in"
              loading="lazy"
            />
          </button>
          <div class="p-6">
            <h3 class="text-lg mb-2">GBP Post</h3>
            <p class="text-brand-dark-light text-sm leading-relaxed">Written and published to your Google Business Profile, targeting a search term patients in your area are actively using that month.</p>
          </div>
        </div>
        <div class="bg-white rounded-2xl overflow-hidden shadow-sm">
          <button class="lightbox-trigger block w-full text-left" aria-label="View trend report example full size">
            <img
              src={`${base}images/recommendations.png`}
              alt="Example monthly trend-based recommendations report"
              class="w-full h-48 object-cover object-top cursor-zoom-in"
              loading="lazy"
            />
          </button>
          <div class="p-6">
            <h3 class="text-lg mb-2">Trend Report</h3>
            <p class="text-brand-dark-light text-sm leading-relaxed">A short, specific document covering what's rising in pelvic floor PT searches and exactly what we should do about it for your practice.</p>
          </div>
        </div>
      </div>

      <!-- Lightbox overlay -->
      <div id="lightbox" class="fixed inset-0 bg-black/80 z-50 hidden items-center justify-center p-4" role="dialog" aria-modal="true" aria-labelledby="lightbox-title" aria-hidden="true">
        <p id="lightbox-title" class="sr-only"></p>
        <button id="lightbox-close" class="absolute top-4 right-4 text-white/80 hover:text-white" aria-label="Close image preview">
          <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
        <img id="lightbox-img" src="" alt="" class="max-w-full max-h-[90vh] rounded-lg shadow-2xl object-contain" />
      </div>

      <script>
        const lightbox = document.getElementById('lightbox');
        const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement;
        const lightboxTitle = document.getElementById('lightbox-title');
        const closeBtn = document.getElementById('lightbox-close') as HTMLButtonElement;
        let triggerBtn: HTMLElement | null = null;

        document.querySelectorAll('.lightbox-trigger').forEach((btn) => {
          btn.addEventListener('click', () => {
            triggerBtn = btn as HTMLElement;
            const img = btn.querySelector('img') as HTMLImageElement;
            // Set content before revealing so screen readers get the right announcement
            lightboxImg.src = img.src;
            lightboxImg.alt = img.alt;
            lightboxTitle.textContent = img.alt;
            lightbox.classList.remove('hidden');
            lightbox.classList.add('flex');
            lightbox.setAttribute('aria-hidden', 'false');
            closeBtn.focus();
          });
        });

        function closeLightbox() {
          lightbox.classList.add('hidden');
          lightbox.classList.remove('flex');
          lightbox.setAttribute('aria-hidden', 'true');
          triggerBtn?.focus();
        }

        // Focus trap — only one focusable element (close button), so prevent Tab from escaping
        lightbox.addEventListener('keydown', (e) => {
          if (e.key === 'Tab') {
            e.preventDefault();
            closeBtn.focus();
          }
          if (e.key === 'Escape') closeLightbox();
        });

        closeBtn.addEventListener('click', closeLightbox);
        lightbox.addEventListener('click', (e) => { if (e.target === lightbox) closeLightbox(); });
      </script>
    </div>
  </section>

  <!-- Pricing -->
  <section class="section-padding" id="pricing">
    <div class="container-max">
      <div class="text-center mb-12 max-w-2xl mx-auto">
        <span class="section-label">Pricing</span>
        <h2 class="section-title">Simple, Transparent Pricing</h2>
        <p class="section-description">
          No hidden fees. No long-term contracts. You know exactly what you’re paying for.
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        <!-- Setup -->
        <div class="bg-white p-8 lg:p-10 rounded-2xl shadow-sm">
          <div class="mb-6">
            <h3 class="text-lg mb-1">{siteConfig.pricing.setup.name}</h3>
            <div class="flex items-baseline gap-1">
              <span class="text-4xl font-heading text-brand-primary">{siteConfig.pricing.setup.price}</span>
              <span class="text-brand-dark-light text-sm">{siteConfig.pricing.setup.label}</span>
            </div>
          </div>
          <p class="text-brand-dark-light mb-6">{siteConfig.pricing.setup.description}</p>
          <ul class="space-y-3">
            {siteConfig.pricing.setup.features.map((feature) => (
              <li class="flex items-start gap-3 text-brand-dark-light">
                <svg class="w-5 h-5 text-brand-accent flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                <span>{feature}</span>
              </li>
            ))}
          </ul>
        </div>

        <!-- Monthly -->
        <div class="bg-white p-8 lg:p-10 rounded-2xl shadow-sm border-2 border-brand-primary relative">
          <div class="absolute -top-3 left-8 bg-brand-primary text-white text-xs font-semibold uppercase tracking-wider px-3 py-1 rounded-full">
            Ongoing Growth
          </div>
          <div class="mb-6">
            <h3 class="text-lg mb-1">{siteConfig.pricing.monthly.name}</h3>
            <div class="flex items-baseline gap-1">
              <span class="text-4xl font-heading text-brand-primary">{siteConfig.pricing.monthly.price}</span>
              <span class="text-brand-dark-light text-sm">{siteConfig.pricing.monthly.label}</span>
            </div>
          </div>
          <p class="text-brand-dark-light mb-6">{siteConfig.pricing.monthly.description}</p>
          <ul class="space-y-3">
            {siteConfig.pricing.monthly.features.map((feature) => (
              <li class="flex items-start gap-3 text-brand-dark-light">
                <svg class="w-5 h-5 text-brand-accent flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                <span>{feature}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>

      <!-- Package Connector -->
      <div class="max-w-2xl mx-auto mt-10 text-center bg-brand-primary/5 border border-brand-primary/20 rounded-xl p-6">
        <p class="text-brand-dark text-base leading-relaxed mb-0">
          <strong>The website build and monthly SEO are designed to work together as a package.</strong> I build your site with SEO in mind from day one, then optimize it month over month. That’s how results compound.
        </p>
      </div>

      <p class="text-center text-brand-dark-light text-sm mt-8 max-w-2xl mx-auto">
        SEO is a compounding investment — most practices start seeing measurable results around month three. But there are no contracts and no commitments. You stay because it’s working.
      </p>
    </div>
  </section>


  <!-- FAQ -->
  <section class="section-padding">
    <div class="container-max">
      <div class="text-center mb-12 max-w-2xl mx-auto">
        <span class="section-label">FAQ</span>
        <h2 class="section-title">Common Questions</h2>
      </div>

      <div class="max-w-3xl mx-auto space-y-4">
        <details class="faq-item">
          <summary>Can I hire you for just a website build?</summary>
          <div class="faq-content">
            <p>The website and monthly SEO work best as a package. I build every site with SEO baked in from day one — the structure, the content, the technical foundation. Then I optimize it month over month so your rankings keep climbing.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>I already have a website. Can you just do SEO?</summary>
          <div class="faq-content">
            <p>I only work on sites I build. That way I control the entire stack — speed, structure, schema markup, content — and I can move fast without waiting on another developer. It also means better results, because the site and SEO strategy are built as one system from the start.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>What happens if I cancel the monthly service?</summary>
          <div class="faq-content">
            <p>Your website is yours. The code, the content, the design — all of it. If you cancel, I hand everything over and you can host it yourself or have someone else maintain it. No lock-in, no hostage situations. SEO optimization stops when the monthly service stops, Your site stays live, though without ongoing optimization those rankings will naturally drift over time as competitors continue investing in theirs.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>How long before I see results?</summary>
          <div class="faq-content">
            <p>Your new website goes live within a few weeks of starting. SEO is a longer game — most practices start seeing meaningful movement in search rankings around month three, with results compounding from there. That said, there are no long-term contracts. You stay because it’s working, not because you’re locked in.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>Do I really own my website?</summary>
          <div class="faq-content">
            <p>Yes, 100%. You own the code, the content, and the design. I keep a backup ready for you at all times with standard instructions for getting it running. If you ever want to part ways, you take everything with you — no proprietary lock-in, no rebuilding from scratch.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>How involved do I need to be?</summary>
          <div class="faq-content">
            <p>As much or as little as you want. You’ll get a plain-English report every month either way. Beyond that, it’s up to you — some clients review blog posts and GBP updates before they go live, others prefer a fully hands-off approach where I handle everything. You can switch between styles anytime. The goal is to take something off your plate, not add to it.</p>
          </div>
        </details>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section-padding">
    <div class="container-max">
      <div class="cta-section max-w-4xl mx-auto">
        <h3 class="text-2xl md:text-3xl">Ready to Get Started?</h3>
        <p class="text-lg max-w-xl mx-auto">
          Let’s have a quick conversation about your practice and figure out the best path forward. No pressure, no sales pitch.
        </p>
        <a href={`${base}contact/`} class="btn btn-primary bg-brand-accent border-brand-accent hover:bg-brand-accent-dark hover:border-brand-accent-dark">
          Get a Free Consultation
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
